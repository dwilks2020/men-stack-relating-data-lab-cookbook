<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home Page</title>
  <link rel="stylesheet" href="/path/to/your/css/styles.css"> 
</head>
<body>
  <h1>Welcome to the Home Page</h1>

  <% if (user) { %>
    <h2><%= user.username %>'s Pantry</h2>

    <% if (pantryItems && pantryItems.length > 0) { %>
        <ul>
            <% pantryItems.forEach(item => { %>
                <li>
                    <strong><%= item.name %></strong> - <%= item.quantity %>
                </li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>There are no items in this pantry.</p>
    <% } %>
  
    <a href="/users">Back to Community</a>
    <p><a href="/vip-lounge">Get into the VIP Users Only lounge!</a></p>
  <% } else { %>
    <p>Please <a href="/auth/sign-in">sign in</a> to access your pantry.</p>
  <% } %>
</body>
</html>
