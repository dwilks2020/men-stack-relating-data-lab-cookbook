<h1>Explore Our Community</h1>
<ul>
    <% users.forEach(user => { %>
        <li><a href="/users/<%= user._id %>"><%= user.username %></a></li> <!-- Assuming username is a field -->
    <% }) %>
</ul>
