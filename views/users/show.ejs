
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= user.username %>'s Pantry</title>
    <link rel="stylesheet" href="/path/to/your/css/styles.css"> <!-- Add your CSS -->
</head>
<body>
    <h1><%= user.username %>'s Pantry</h1>

    <% if (pantryItems.length > 0) { %>
        <ul>
            <% pantryItems.forEach(item => { %>
                <li>
                    <strong><%= item.name %></strong> - <%= item.quantity %>
                </li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>There are no items in this pantry.</p>
    <% } %>

    <a href="/users">Back to Community</a> <!-- Link back to community page -->
</body>
</html>
